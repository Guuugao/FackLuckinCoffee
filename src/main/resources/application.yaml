server:
    port: 10000

spring:
    application:
        name: FackLuckinCoffee
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/luckin
        username: root
        password: root
        type: com.alibaba.druid.pool.DruidDataSource


mybatis-plus:
    configuration:
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
        map-underscore-to-camel-case: false # ??????????????
        jdbc-type-for-null: 'null'

sa-token:
    # token ??????? cookie ???
    token-name: saToken
    # token ????????? ??30??-1 ??????
    timeout: 604800 # 7?
    # ?????????????? ?? true ???????, ? false ??????????
    is-concurrent: false
    # ????????????????? token ?? true ????????? token, ? false ????????? token?
    is-share: true
    # token ?????????uuid?simple-uuid?random-32?random-64?random-128?tik?
    token-style: uuid
    # ????????
    is-log: true

logging:
    level:
        root: info
